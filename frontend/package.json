 {
  "name": "vulnerable-chatbot-frontend",
  "version": "1.0.0",
  "description": "Frontend for vulnerable chatbot demo",
  "scripts": {
    "start": "npx live-server --port=3000 --host=localhost"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}